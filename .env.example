# Optional Hugging Face token for gated models.
# Prefer passing it through your shell or VM environment instead of committing it.
HF_TOKEN=

# Local tunnel/API defaults.
TTS_API_BASE_URL=http://localhost:8091
